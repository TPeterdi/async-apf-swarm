package async.apf.model.enums;

// Enum for different robot event types
public enum SimulationEventType {
    ROBOT_LOOKING, ROBOT_COMPUTING, ROBOT_MOVING, ROBOT_IDLE, SIMULATION_START, SIMULATION_END, SIMULATION_FAIL
}
